# Firebase Configuration (Required for app functionality)
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_auth_domain_here
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_storage_bucket_here
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id_here

# Firebase Admin SDK (Required for server-side Firebase operations)
# Note: The private key should be properly escaped with \n for newlines
FIREBASE_CLIENT_EMAIL=your_client_email_here
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour Private Key Here\n-----END PRIVATE KEY-----\n"

# Admin and Migration API Security
ADMIN_API_KEY=choose_a_secure_random_string_for_admin_routes
MIGRATION_SECRET_KEY=choose_a_secure_random_string_for_migration_routes

# AI API Keys (Optional - only needed if using these features)
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
REPLICATE_API_TOKEN=your_replicate_api_token_here
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# Other API Keys
# Add other API keys as needed for your project

# IMPORTANT SECURITY NOTES:
# 1. Never commit your actual .env or .env.local file to version control
# 2. Regenerate all API keys if they have been accidentally exposed
# 3. Use strong, random values for admin and migration keys
# 4. In production, consider using a secrets management service
# 5. This example file is meant to show the required environment variables without exposing actual credentials 